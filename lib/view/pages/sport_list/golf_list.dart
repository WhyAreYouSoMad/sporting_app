import 'package:flutter/material.dart';

class GolfList extends StatelessWidget {
  const GolfList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text("리스트가 들어올자리"),
      ),
    );
  }
}
