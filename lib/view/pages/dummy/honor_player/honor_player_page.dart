import 'package:flutter/material.dart';

class HonorPlayerPage extends StatelessWidget {
  const HonorPlayerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(),
    );
  }
}